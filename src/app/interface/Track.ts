export interface Track {
    // Define the properties for the Track entity
    // For example:
    trackId: number;
    title: string;
    duration: number; // or string, depending on your backend model
}